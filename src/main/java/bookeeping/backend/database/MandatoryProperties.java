package bookeeping.backend.database;

public enum MandatoryProperties
{
	nodeId, rootNodeId, next, userId, filesystemId, directoryPath, directoryName, filePath, fileName, commitId, version, permission
}
